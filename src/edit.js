// WordPress dependencies.
import { __ } from '@wordpress/i18n';

export default () => (
	<>
		<h1>{ __( 'Hello World', 'wpse-366881' ) }</h1>
		<p>{ __( 'Yay, it works!', 'wpse-366881' ) }</p>
	</>
);
